let form = document.getElementById('f');
form.addEventListener('submit', (event) => {
    event.preventDefault();

    let username = form.elements[0].value;
    let email = form.elements[1].value;
    let title = form.elements[2].value;
    let category = form.elements[3].value;
    let question = form.elements[4].value;

    console.log("hi ");

    axios.post('http://localhost:3000/api/newquestion', { username: username, email: email, title: title, category: category, question: question }).then(response => {

        let result = response.data;

        console.log(result);

        alert("ddan");
        window.location = 'http://127.0.0.1:5500/index.html';



    })
        .catch(error => console.error(error));

}); 